# Clustering extras for Part 1/3 Scanpy execution and environment gates.
# Helper CLIs in this repository do not import these packages.
# Install with: python -m pip install -r requirements-part1.txt
-r requirements.txt
scrublet>=0.2,<1
harmonypy>=0.0.10,<1
igraph>=0.10,<2
leidenalg>=0.9,<1
